name: Seed Forecast Accuracy

on:
  schedule:
    - cron: '10 1 * * *'        # ~10 mins after retrain
  workflow_dispatch:

permissions:
  contents: write               # needed to push DB back to repo

concurrency:
  group: seed-accuracy-${{ github.ref }}
  cancel-in-progress: true

jobs:
  seed-accuracy-db:
    runs-on: ubuntu-latest
    env:
      PYTHONUNBUFFERED: "1"

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'
          cache: 'pip'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Sanity check paths
        run: |
          echo "CWD:"; pwd
          echo "Top-level:"; ls -la
          echo "forecast_eval/:"; ls -la forecast_eval || true
          echo "CSV at repo root?"; test -f forecast_metrics.csv && echo "✅ found" || echo "❌ missing"

      - name: Show seed_db.py header & help
        working-directory: forecast_eval
        run: |
          sed -n '1,120p' seed_db.py || true
          echo "---- seed_db.py --help ----"
          python -u seed_db.py --help || true
          echo "---------------------------"

      - name: Seed accuracy DB
        working-directory: forecast_eval
        run: |
          set -euxo pipefail
          CSV="../forecast_metrics.csv"
          OUT="forecast_accuracy.db"

          # If metrics CSV isn't present yet, don't fail the pipeline
          if [ ! -f "$CSV" ]; then
            echo "No $CSV found; nothing to seed. Exiting cleanly."
            exit 0
          fi

          # Try multiple common CLI signatures until one succeeds
          (
            python -u seed_db.py --csv "$CSV" --out "$OUT" \
            || python -u seed_db.py --csv "$CSV" --db "$OUT" \
            || python -u seed_db.py --metrics "$CSV" --db "$OUT" \
            || python -u seed_db.py --input "$CSV" --output "$OUT" \
            || python -u seed_db.py "$CSV" "$OUT" \
            || python -u seed_db.py --csv "$CSV" \
            || python -u seed_db.py
          )

          # Verify DB was actually produced
          test -f "$OUT"

      - name: Move DB to repo root
        working-directory: forecast_eval
        run: mv -f forecast_accuracy.db ..

      - name: Commit & push seeded DB
        if: success()
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "actions@github.com"
          git add forecast_accuracy.db
          git commit -m "Nightly: update forecast_accuracy.db ($(date -u +'%Y-%m-%d'))" || echo "No changes to commit"
          git push
