# Secrets — NEVER commit
.streamlit/secrets.toml

# Large model files — retrain on deployment
models/saved/*.joblib
models/saved/*.pkl

# Databases — local only
*.db
*.sqlite

# Python cache
__pycache__/
*.pyc
*.pyo
*.pyd
.Python

# Mac junk
.DS_Store
**/.DS_Store

# Env
.env
.venv
venv/

# Alerts __init__ (wrong name)
alerts/alerts__init__.py
.streamlit/secrets.toml
data/cache/
data/intraday_history/
logs/
models/saved/regime_cache.json
data/cache/alpha/
.portfolio.enc
# Trained models — regenerated from training pipeline
models/*.joblib
models/*/*.joblib
# Reports — regenerated from scripts
reports/*.csv
reports/*.log

# === Backup hygiene (added 2026-05-01) ===
*.bak
*.bak.*
*.bak_*
models/saved.bak*/
*.db.bak.*
patch_*.py
archive/

# institutional_trades.db SQLite WAL files
institutional_trades.db-shm
institutional_trades.db-wal

# Step 4 correlation analysis artifacts (May 7 2026) — diagnostic only
feature_correlation_full.csv
feature_panel_sample.csv

# Model backup directories (~350MB each, would bloat repo)
models/saved/backup_before_*/

# Local fitness scoring CSV (regenerated weekly via cron)
fitness.csv
fitness_relaxed.csv
recession/refresh_log.txt
recession/cron.log
recession/recession_alerts.log
recession/recession_alerts.log
recession/recession_alert_state.json

# P0-2 temp artifacts (regenerable)
p0-2_*.db
p0-2_*_panel.parquet
p0-2_*.log

# SQLite sidecars
*.db-wal
*.db-shm
*.db.bak.*
# backup / scratch artifacts
*.bak2
*.deadfix.applied
*.predeadfix*
apply_*_patch.py
task_d_stage*.py
audit_phase*
signal_discrepancy_audit*.py
# regenerable caches
recession/cache/
data/alpha_panel/
institutional_trades.duckdb

# generated backtest/fitness outputs
fitness.csv
reports/walkforward_*.csv

# but DO track the dated archive (decay history)
!reports/archive/
!reports/archive/*.csv
sec_filings.db
data/osap/
*.db-shm
*.db-wal
*.db-journal
fundamentals.db
prices.db
prices.db
