# ───────── core ─────────────────────────────────────────────
setuptools==69.5.1          # keep first – uv/pip rely on it
python-dotenv==1.0.1        # env-vars for secrets

# ───────── data stack ───────────────────────────────────────
numpy==1.26.4               # latest 1.x wheel → fast install
pandas==2.3.1
scikit-learn==1.7.1         # still pure wheel on Py 3.11
xgboost==1.7.6              # binary wheel, no compile step

# ───────── viz / UI ─────────────────────────────────────────
streamlit==1.47.1
streamlit-extras==0.4.7     # pin to a Py 3.11 wheel
altair==5.5.0
matplotlib==3.10.3

# ───────── helpers ──────────────────────────────────────────
yfinance==0.2.65            # market data
pandas_ta==0.3.14b0         # technical indicators (pure-python)
holidays==0.25              # US trading-day calendar
tqdm==4.67.1                # nice progress bars
requests==2.32.4            # HTTP backbone

# ───────── Google-Sheets logging (optional) ────────────────
gspread==6.2.1
oauth2client==4.1.3
google-auth==2.40.3
google-auth-oauthlib==1.2.2

# ───────── misc runtimes  ──────────────────────────────────
fsspec==2024.6.1            # file-like I/O used by pandas
tenacity==9.1.2             # retry decorator
