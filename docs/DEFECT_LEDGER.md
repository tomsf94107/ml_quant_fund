# MONITOR DEFECT LEDGER — single source of truth
*Both threads (pipeline-engineering AND report-writing) read this before writing any §4 / §16.4
and append here when shipping or discovering. Report §4 renders FROM this file; a monitor pull
alone is NOT sufficient evidence a defect is open — several fixes are invisible in stdout.*
*Format: one row per defect, never deleted. Status ∈ OPEN / FIXED / SUPERSEDED / DEAD / GATED.*
*Last sync: 2026-07-26 (session: report-audit + fix batch, commits c6b6e636..c32394db).*

| Item | Status | Commit / evidence | Notes |
|---|---|---|---|
| UW /ohlc/1d 3-rows-per-session (returns, spot) | FIXED | f4b6ce6d | `_daily_returns` from prices.db; IWM→SPY |
| VN date off-by-one (`date.today()`) | FIXED | c6b6e636 | `_today_et()` at 11 sites |
| Stale earnings_date anchors front weekly | FIXED | b1eb33b2 + guard-routed 7e8ae039-era | `section_implied_move` routed through guard |
| Form 4 other-issuer (GV) misattribution | FIXED | cb294e5a | **CIK filter — supersedes any issuer-price heuristic. STRIKE issuer-price BUILD** |
| form4_transactions dupes / case / no unique index | FIXED | 7e8ae039 | 914 rows deduped, `tsla`→TSLA, `uq_form4_tx` permanent |
| EPS contamination GOOG 07-22/04-29 (GAAP marks) | FIXED | (EPS batch) | DB NULLed + writer + display quarantine + >100% tripwire |
| EPS basis mismatch DDOG (GAAP vs adj, systematic) | FIXED | 7e8ae039 | ticker-level quarantine tier |
| Missing `street_mean_est` key (Est all "?") | FIXED | (EPS batch) | also revived surprise + tripwire |
| Options aggressor side dropped | FIXED | (tilt commit) | ask/bid tilt + A% + vendor OPENING tag |
| Dark-pool partial days (13-ticker universe) | FIXED | repair runs Jul 24-26 | 0 partials all tickers; pre-2026-05-29 permanently partial (vendor cap) |
| Top-10 prints rank AH/mechanical as organic | FIXED | 379749f7 | AH/MECH tags |
| Skew universe silent (57% specimen) | FIXED | 379749f7 | signed-universe line |
| Closing crosses inside signed skew (PLTR claim) | DEAD | probe Jul 24 | **REFUTED** — ext_hours excluded them all along |
| clean_skew wiring into 7d aggregate | DEAD | — | justifying defect refuted; not shipped |
| 1d-move column all "?" | FIXED | (batch Jul 26) | prices.db backfill, `*` marker, normalize-gate bugfix |
| Insider rolling-window denominator artifact | FIXED | (batch Jul 26) | fixed 90d/365d totals printed |
| Put-call parity flags cost-of-carry | FIXED | (batch Jul 26) | C−P = S−K·e^(−rT), r=4.5% |
| PARTIAL label misleads on healed windows | FIXED | (batch Jul 26) | label states live-walk-stops-at-cache |
| Generic-prior historical-move category error | FIXED | c32394db | RICH/CHEAP suppressed under generic prior |
| GOOG cohort undefined | FIXED | c32394db | MSFT/AMZN/META; first read −13.44pp/20d |
| Live-book membership not printed (§16.2 blocker) | FIXED | c32394db | LIVE BOOK line in every ticker header |
| Dark-pool skew predictive validation | GATED | 23849ec6 + cron aa777f86 | v1.0 live; 2026-07-26 run: UNDERPOWERED (8 dates, h=1 IC +0.138, t 1.54, z 1.2σ); monthly on the 12th; bar \|z\|≥3 ∧ \|NW-t\|≥3 both horizons |
| DB lock crashes (Jul-17, Jul-25) | FIXED | timeout=30 (parallel session) + iCloud relocation | WAL deliberately withheld until post-migration verify |
| 10-Q extraction | OPEN | — | sole BUILD-PRIORITY; test accession 0001652044-26-000066 |
| Intraday session H/L | OPEN | — | blocks range analysis; external substitutes class-mismatched |
| GOOG next-earnings date 3-way conflict | OPEN | — | monitor 2026-11-04 (UW placeholder [inf]) vs external Oct 27–28; update on Alphabet announcement |
| Peer-relative DORMANT root-cause | OPEN | — | 5 clean runs; clean ≠ fixed |
| UW news 404 / Massive session_reset | COSMETIC | — | vendor-plan / log-only |
| Cohort peer-residual factor | RESEARCH | — | REV3 candidate; goes through the standard gate (null control, HLZ t>3) before any sink |
| Aggressor-tilt validation | BLOCKED | — | needs flow-alert persistence build first; then reuses skew gate via --signal |
| Block desynchronization (3 prices/run) | FIXED | (this commit) | staleness fall-through + bar-date print; universal via _daily_returns |
| NVDA/DDOG/BYND earnings dates | FIXED | (this commit) | all company-PR/WSH confirmed; OKLO QUBT CRWD SNOW NVMI remain stale-guarded, batch-source before windows |
| RZLV dark-pool 7-session gap (13-22 Jul) | FIXED | repair Jul 29 (347 rows, 0 partials) | RZLV not in DEFAULT_TICKERS -- monthly repair skips it, heal manually or add |
| si_live_ledger staleness (all reports) | CORRECTED Aug-31 | **A GENERATOR EXISTS**: `si_positions_live.py --log-ledger --capital N` (lines 344-402). The prior 'hand-maintained, no generator' note was WRONG and misled every downstream thread. | NOT a code defect -- Atom re-dates/refreshes; report threads must stop flagging as monitor bug |
| Implied-move published on broken chains | FIXED | (this commit) | parity before print; number suppressed, not caveated |
| Spot/chain clock mismatch post-event | FIXED | (this commit) | re-anchor to live quote past 2pct divergence |
| earnings_date coverage (13 hand names) | FIXED | (this commit) | earnings_calendar fallback = 312 tickers; hand config wins when fresh |
| AMD + MRVL HELD in si_live_ledger | NOTED | grep Jul-31 | 7sh AMD / 14sh MRVL, SI long leg -- those reports ARE position-relevant |
| DB locks (Jul-17, Jul-25) | FIXED | dfa50620 | 108 bare sqlite3.connect sites, 65 files -- default 5s timeout meant collisions crashed instead of waiting; ui/ concentration matches the dashboard long-read note |
| Scheduler duplicates | FIXED | 245c4e5e + inventory | 3 jobs double-owned Jul 26-Aug 1 (my install resurrected them); chain now holds an atomic lock |
| Destroyed crontab jobs (Jul-26) | FIXED | 800b6f8a | 11 jobs restored incl. feed_freshness_check; install_crontab.sh refuses destructive installs, crontab_drift_check.sh on launchd |
| health_check false alarms | FIXED | 9f26efcd + 02ddf15b | 77 consecutive false failures, 0 passes ever; maturity-aware now, status file + pipecheck age display |
| cron_canary | REMOVED | dfa50620 | echo-only stub, 2880 runs/day |
| 24-vs-50 form4 count | UNEXPLAINED | -- | overwrite hypothesis REFUTED (5 quarantine markers durable, 3 new catches). No replacement story |
| momentum_shadow 6/29-7/9 hole | RECOVERED | Aug-1 | 8 sessions never written (Jun 429-storm froze the panel; the frozen-panel bug re-wrote 6/23 picks nightly, fixed 7-14 but gap never backfilled). Regenerated + 4 windows matured |
| momentum_shadow ETF membership | NOT A DEFECT | Aug-1 | 9 ETFs in tickers.txt, in the book since its FIRST date (2026-05-29); prices.db has carried them since 2022. build_panel's "correctly excluded as uncached" comment was FALSE, now corrected. Impact: 2.3pct of cross-section, ONE ETF BUY in all history (SLV 06-12). The 18yr gauntlet pivots the same daily_prices unfiltered -- shared universe, no parity gap. Three prior explanations (survivorship tilt, evaporated safeguard, construction gap) were all REFUTED by measurement |
| momentum_shadow membership drift | CAVEAT | Aug-1 | Regenerated dates use load_universe() = TODAY (785 rows) vs June-era dates (769); 16 tickers added, 2 dropped, 6/26 is a merged 787. Price panel is PIT (panel[index<=cutoff]) but MEMBERSHIP is not. Cross-sectional decile cuts span different populations pre/post 6/26 -- read any gate verdict with this in view |
| EPS vendor basis mismatch | FIXED (detector) | Aug-1 | 37 tickers show 4+ quarters ALL negative avg < -10pct -- vendor actual is GAAP-ish, estimate is street non-GAAP. Per-ticker signature detection at render; hand blocklist does not scale. Landmine for any future SUE work: 37 of ~400 names carry systematic fake-negative surprises |
| AMZN Form-4 undercount | RETRACTED | Aug-1 | 0.51M = 14-day window, 37.72M = 90-day window, both correct. Parser fine; the report compared different denominators |
| WAL journal mode | DONE | Aug-1 | earnings_monitor.db + short_interest.db were the only rollback-mode DBs; the former is where both lock crashes occurred. Backups must copy .db + -wal + -shm |

## RESEARCH PRECONDITION (Aug 1 2026)

PEAD/SUE work: `earnings_cache.actual_eps` is UNUSABLE as a surprise input for ~37 of 400 tickers -- the vendor actual is GAAP-ish while est_eps is street non-GAAP, producing 4-7 consecutive fake double-digit misses (SNOW -440pct, NET -135, CRWD -104, MRVL -22, AMD -19). Any SUE construction must source actual and estimate from the SAME basis. PEAD is already VOID for look-ahead; this is a second, independent poisoning of the same input.
| Earnings-quality items unparsed (10-K/10-Q) | OPEN | found by MSFT report Aug-1 | Monitor missed a $3.2B Anthropic equity gain and a 15->25yr useful-life change in MSFT FY26 10-K -- both materially alter how GAAP EPS reads. Same family as 10-Q extraction; raises its priority |
| Ledger unreachable from report threads | FIXED | scripts/ledger_digest.sh | Report chats have no repo access; digest prints OPEN rows + commit hash, pipe to pbcopy and paste |
| BLOCK flag absolute-dollar | FIXED | Aug-1 | Now max(1M floor, 3x median daily max print) per ticker. MSFT threshold 619M (was flagging every session), NVMI 19M (was flagging none) |
| Intraday session H/L | FIXED | Aug-1 | raw_bars carried high/low all along (420 tickers, 0 nulls); now printed with range pct + close-position-in-range. No external sourcing needed |
| Aggressor-tilt validation | UNBLOCKED (accumulating) | Aug-1 | flow_alerts persists per-alert ask/bid premium each run. CAVEAT: the UW endpoint is COUNT-limited (limit=100), not time-limited -- MSFT 90 alerts = 1 session, NVMI 58 = 23 sessions. Liquid names capture ~1 session per run and lose any day the monitor is not run; thin names backfill weeks. Date coverage will fill fastest where the signal matters least |
| Peer-relative DORMANT root-cause | CLOSED | Aug-1 | Original defect structurally gone: every row routes through _daily_returns, so no second path can disagree (explains 5 clean runs). Wait-and-see replaced by a bar-date assertion -- panel states its close date or warns on mismatch, catching the real residual (rows spanning different sessions) |
| 10-K/10-Q earnings-quality extraction | FIXED | Aug-1 | section_periodic_content, universe-wide. Cat-1 non-operating income (MSFT: 6.5B OpenAI net gains) and cat-2 estimate changes (prose gate + past-tense change verbs). No HTML table parsing by design. Coverage: 66 of ~400 tickers have periodic filings cached, grows as section_edgar runs |
| MSFT report earnings-quality figures | CONTESTED | Aug-1 | Report claimed a 3.2B Anthropic gain and a 15->25yr useful-life change. The FY26 10-K says 6.5B net gains from OPENAI and states servers at two to six years with no revision language. Report figures appear misattributed -- route to the MSFT thread for correction |
| Implied move off-ATM intrinsic | FIXED | Aug-3 | Straddle minus |S-K| intrinsic; parity cannot catch this (off-ATM chains satisfy C-P=S-K exactly). BYND: +/-24.81pct RICH 2.57x became +/-7.53pct fair 0.77x -- inverted verdict on a name printing in 2 days. Worst on low-priced tickers with coarse strike grids |
| Peer panel prints SPY twice | OPEN | seen on BYND Aug-3 | Tickers with no sector_etf default to SPY, which is also the market benchmark -- two identical rows and two vs-SPY lines |
| EPS >100pct tripwire false-positives on loss-makers | OPEN | seen on BYND Aug-3 | est -0.12 vs actual -0.29 = -142pct is a legitimately wider loss, not contamination; pct surprise is meaningless on a small negative denominator |
| BLOCK floor too high for sub-dollar names | OPEN | seen on BYND Aug-3 | max(1M floor, 3x median) means BYND can never flag (3x median = 442K); a 575K print at 3.9x its median max went unflagged. Residual of the Aug-1 relative-threshold fix |
| squeeze_mult overlay live in prob_eff | FIXED | 075dae6e | Hand-weighted SI multiplier boosted prob_eff up to 1.15x on HIGH short interest -- OPPOSITE sign to the one validated brick (71pct of SI edge is the LOW-DTC LONG leg; high-DTC short leg Sharpe +0.67, not significant). Live since 2026-05-08. Measured from accuracy.db: 432 signals crossed ENTRY ONLY because of this multiplier (h=1:7, h=3:203, h=5:222) across 12 tickers (ORIC, AI, LYFT, ALT, BYND, VKTX, QUBT, ASAN, ECHO, FOXA, GEMI, LEU). Never gated on per-date IC / NW-t / shuffle null. Now env-gated OFF (ML_QUANT_ENABLE_SQUEEZE_MULT), mirroring the intraday_mult precedent. The 0.90 BEARISH_TRAP branch (345 rows) agrees with the brick's sign and was gated with it, not deleted. RULE-13: PROCESS |
| SI scalar broadcast into pooled fits | FIXED | c87947ac | builder.py:1394 assigned mc.get_short_interest()'s CURRENT value as a SCALAR to every row of the ticker's frame. Inert in per-ticker fits (constant => zero splits); a look-ahead ORACLE in pooled fits -- train_global_ranker.py:67 concats across tickers with n_groups=1111 / avg_group_size 384, i.e. groups are DATES, so the model learned "rank these names up because they are heavily shorted as of today" applied to 2021 rows. Measured leaked gain: short_ratio 0.84/1.53/2.62pct + short_pct_float 0.46/0.66/0.96pct at h=1/3/5, monotonic in horizon. All three GLOBAL_ranker artifacts VOID for evaluation until retrained. Contained: prob_up_global_ranker is logged-only, never in the line-872 prob_eff product. Replaced by _load_short_interest_pit(): merge_asof on settlement_date + 9 weekdays (FINRA publishes on the 7th BUSINESS day; conservative holiday-blind proxy), NaN fill not 0.0, one path with no training_mode branch. Seven models/* callers still omit training_mode=True. RULE-13: PROCESS |
| UW total_float is shares OUTSTANDING | OPEN | Aug-26 | UW /api/shorts/{t}/interest-float/v2 returns a field named total_float that is actually shares outstanding. Verified: GME total_float 448,691,257 == shares outstanding exactly (independent sources); true free float ~409.4M, insider ownership ~10.7pct. si_float is denominated on it, so every SI-percent surface UNDERSTATES the real squeeze ratio -- ~9pct relative on GME, far more on heavy-insider names. features/short_interest.py compares short_pct_float to SHORT_PCT_EXTREME=0.20 as if float-denominated; all thresholds in that file are biased low. squeeze_radar withholds the figure entirely. Remaining surfaces must be relabelled "pct of outstanding". Free-float source candidate: 10-K cover-page aggregate market value held by non-affiliates / price on that date |
| Splits cache TTL blind to corporate actions | FIXED | f64faff4 | massive_client.py:701 cached split lookups 30 days INCLUDING EMPTY results. A positive result is settled history; an empty result is a claim about the FUTURE. BYND cached [] on 2026-08-03; its 1-for-30 reverse split (SEC 8-K, effective 2026-08-14) stayed invisible until 2026-09-02. Expected miss window under a flat 30d TTL is ~15 days -- BYND was the base rate, not bad luck. daily_prices carried a fake +3,110pct single-day return, feeding validate_si_v2 / momentum_shadow / combine_mom_si / audit_ic. Massive's API had the split all along. Fix: TTL 30d positive / 2d empty; ticker.upper() on read+write (splits_cache held 'SMCi' and 'MSFt' as keys distinct from SMCI/MSFT, two caches that never saw each other's data). Sweep of 255 empty-cache tickers: 0 further gaps. Repair: splits += BYND 30:1; daily_prices rebuilt 2026-08-01+ and pre-Aug history rebased x30 (1,822 rows); discontinuity scan since 2024 clean. RULE-13: DATA |
| Bar-inferred split detector (retracted) | RETRACTED | f64faff4 | Interim heuristic inferred splits from bars via price_ratio*volume_ratio ~ 1. Passed a Feb-2026-onward sample (12 events, clean separator 0.60-1.66 splits vs 11.9-1390 real) and FAILED on full history: flagged GME 2021-01-26/27/29 and AMC 2021-06-02 -- the canonical short squeezes -- as splits, INSIDE a short-squeeze scanner. Implied ratios 1.60-2.35; real split ratios are round. Also ran ~10pct hot on BYND (33.10 vs true 30) because close-to-close includes the day's real move. prices.db:splits is authoritative; heuristic demoted to flag-only (>=5x or <=0.2x suppresses dtc_live/rvol/ret_3d/ignition rather than adjusting) |
| squeeze_scan date.today() 403s | FIXED | 1218a2d6 | VN-local date runs AHEAD of the ET session calendar 00:00-11:00 VN (13:00-24:00 ET), requesting a daily bar that does not exist -> Massive NOT_AUTHORIZED. Ran on EVERY monitor invocation via the shell wrapper (line 84), not just direct calls, so the afternoon/evening ET report window was systematically hit. All defaults and user-supplied --end now clamp to America/New_York |
| squeeze_scan wiring notes rendered as data | FIXED | 1218a2d6 | enrich_fuel() imported fetch_options_flow / fetch_darkpool from scripts/monitor_ticker.py. NEITHER EXISTS (only assess_squeeze:4283, section_squeeze:4343). A bare except Exception swallowed the ImportError and printed a "WIRING:" string into the FUEL CONTEXT panel where it read like data, on every run since the file was written. Now states NOT WIRED explicitly |
| VIX3M / ES=F yfinance blocked | OPEN | Aug-26 | Every build logs "index symbol ^VIX3M: yfinance disabled (XProtect block), returning empty" and the same for ES=F. builder.py:1408 computes (_vix_s / _vix3m_s).dropna() -- the VIX term-structure feature is silently NaN or constant on every panel build. Unmeasured; needs its own thread |
| adj_close basis changed at yfinance->Polygon migration | OPEN | Aug-31 probe | `massive_client.py:481` maps auto_adjust to Polygon `adjusted=`, which is SPLIT-ONLY. Pre-2026-06-29 rows carry split+dividend; everything since is split-only. KO 2024-01-02: legacy 55.6394 vs `_download` 59.82. The `fetch_and_pead.py:100` comment asserting equivalent auto_adjust=True semantics is FALSE. Affects every return computed off daily_prices since June, incl. PEAD. No dividend source in the stack. |
| --rebuild-from wiped dividend adjustment | FIXED | Aug-31, restored from prices.db.bak.20260826_192308 | My `--rebuild-from 2025-08-01` DELETEd + re-derived 52,991 rows / 300 tickers as split-only. Row-level UPDATE from the pre-rebuild backup; verified 0 rows differ, KO 2025-08-04 back to 67.0608. `--rebuild-from` is a latent trap for anyone: sync cannot reproduce dividend adjustment. |
| add_ticker chains no consumer prerequisites | OPEN | Aug-31 session | FOUR hardcoded rosters silently excluded new tickers: FINRA universe (earnings_surprises), sync watermark, ETF_EXCLUDE, SECTOR_ETF_MAP. Instances fixed; the PATTERN is not. TICKER_CONFIG sector_etf (12 of 411) is a fifth. |
| Leverage guard `_open_usd` monotonic | FIXED | 556c1e45 + 9271998f | Guard summed EVERY ledger row forever; `hold_days` written but never read, so bookings would be blocked permanently after the first cohort. `close_cohort.py` moves filled cohorts to si_ledger_closed.csv. Write path tested on /tmp copy: 27 rows moved, P&L reconciles (-8.66%). |
| daily_prices watermark gap (universe-wide) | FIXED | Aug-31 backfill | 53 tickers had raw_bars rows absent from daily_prices; sector SPDRs held 1,168 of 2,544 (~2022+), so sector-relative pre-2022 had NO benchmark. 49 backfilled, split math verified to 1e-7 vs the splits table. DOW/FOX/FOXA/IR excluded (predecessor entities). |
| SPCX short-interest splice | FIXED (durable) | Aug-31, 41c43f12 | 38 SI rows before 2026-06 belong to the AXS SPAC ETF (renamed SPCK 2026-04-07). Purged; 5 genuine settlements remain. `REPLACE INTO` restores them on any fetch_log clear. Prices were already clean from the Aug-17 repair. |
| Stale-price ranking | FIXED | 4ad0d9cf | Names ranked on frozen prices with no warning (AVB 10td -- actually the AVB/EQR->VMRK merger). `--max-px-lag-td` gate + explicit drop reporting for both stale and no-price cases. |
| pytest collection aborts at repo root | FIXED | 850a2e83 | ~26 argparse scripts match test globs and SystemExit during collection, killing the whole run. `pytest.ini` scopes to tests/ warning/ recession/tests. 351 pass. Recorded '89 tests' was stale. |
| recession/tests/test_schema.py `db_path` fixture | OPEN | Aug-31 | Fixture defined nowhere in the repo; no conftest.py at any level. Those 7 tests have never run. |
| Sub-$1 names in the ranked book | DECISION | Aug-31 | CERO $0.0106 = 119,684 shares for a $1,265 slot. `validate_signal.py` has NO price filter, so the brick was validated INCLUDING these. A floor is a deliberate deviation, not a fix. 4 names under $1, 16 under $5. |
| Ticker reuse: entity-date registry | SHIPPED | 41c43f12 | `ticker_entity_dates.csv` + `apply_entity_dates.py`. Purges rows predating a ticker's current entity across short_interest, daily_prices, raw_bars. SPCX (38 SI rows) and BETR (583 daily_prices rows) registered. MUST run after every SI fetch -- REPLACE INTO restores them otherwise, demonstrated same session. VXRT suspected (3x step 2011-07-18, 2018 reverse merger) but handover date unconfirmed -- not registered. |

